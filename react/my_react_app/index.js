import React from 'react'
import ReactDOM from 'react-dom/client'
import MonPremierComposant from "./jsx/MonPremierComposant.jsx";

ReactDOM.createRoot(document.getElementById('react-app')).render(
    <React.StrictMode>
        <MonPremierComposant />
    </React.StrictMode>,
)